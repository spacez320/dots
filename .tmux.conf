# .tmux.conf
#
# Configuration for tmux.

# pane movement
bind-key j command-prompt -p "join pane from:" "join-pane -s '%%'"
bind-key s command-prompt -p "send pane to:" "join-pane -t '%%'"

# sessions
bind r source-file ~/.tmux.d/rabbit

# powerline
source "$POWERLINE/tmux/powerline.conf"
