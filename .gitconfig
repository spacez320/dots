[user]
  email = matthew@eudaimonia.io
  name = Matthew Coleman
[core]
  excludesfile = /home/mcoleman/.gitignore_global
[alias]
  cf = "!git add . && git commit -a --amend --no-edit"
  dc = "diff --color"
  gp = "!source ~/.rc.d/git && gen_patch"
  pf = "!git cf && git push -f"
  rd = "!git fetch -qp && for b in `git for-each-ref --format='%(refname:short)' refs/heads/`; do git branch -r | grep $b | grep -v \"HEAD\"; done"
  rf = "!git stash -u && git pull --rebase origin master && git stash pop"
  ss = "stash save -u"
  su = "!git gc && git fetch -p && git pull --rebase origin master"
  vd = "difftool"
[diff]
  tool = vimdiff
[push]
  default = simple
