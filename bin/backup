#!/bin/bash

main() {

  local checksum=$HOME/checksum/checksum

  # checksum
  dd if=/dev/urandom of=$checksum bs=1M count=10 2> /dev/null

  # backup
  obnam --quiet backup && \

  # verify
  obnam --quiet verify `dirname $checksum` 1> /dev/null

  # forget
  obnam forget

}

main
